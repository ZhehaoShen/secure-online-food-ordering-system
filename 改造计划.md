# Secure / Vulnerable 双系统对照实验改造计划

## 1. 核心定位与设计原则

本项目旨在提供一个**真实的 Web 安全双系统对照实验**（Twin Systems A/B Comparison），而非“安全知识展示网站”或教学文档型应用。

### 核心原则
1. **真实系统对照**：两个系统均为完整的 **Northstar Kitchen 餐饮点餐系统**，在外观、导航结构、路由路径、表单字段、测试数据和用户操作流程上保持高度一致。
2. **独立双端口运行**：
   - **Secure 应用**：`http://127.0.0.1:3000`
   - **Vulnerable 应用**：`http://127.0.0.1:3100`
3. **无缝双窗口操作**：演示者通过双开浏览器窗口（左侧 Secure 3000，右侧 Vulnerable 3100），执行完全相同的输入和点击操作，直观展示安全机制带来的行为与结果差异。
4. **移除教学/文档型导航**：**不新增** `Security Demo` 导航菜单，也**不出售/不制作** `/security-demo` 知识说明主页。所有安全演示直接在真实业务页面（`/login`, `/search`, `/admin` 等）或通过浏览器 DevTools、终端日志及代码审查完成。
5. **紧凑醒目的状态标识**：
   - Secure 端 Header：`SECURE APPLICATION · PORT 3000`（绿色主题徽章）
   - Vulnerable 端 Header：`INTENTIONALLY VULNERABLE · LOCAL DEMO · PORT 3100`（红色/橙色主题徽章 + 本地隔离提示）

---

## 2. 演示对照矩阵 (按照 Project demo instructions.md 顺序)

演示流程严格匹配 `Project demo instructions.md` 要求，依次演示以下 5 大主题：

| 演示环节 (Instruction 顺序) | 操作 / 测试点 | Secure 端 (Port 3000) 行为 | Vulnerable 端 (Port 3100) 行为 | 验证与展示手段 |
|---|---|---|---|---|
| **1. Login SQL Injection** | 1. 输入正常账号密码<br>2. 输入 SQL 注入 payload<br>(如 `' OR '1'='1`) | 1. 正常登录<br>2. 登录失败（提示 `"Invalid email or password"`）；后端采用 SQL 参数化绑定 (`$1`, `$2`) 防范注入 | 1. 正常登录<br>2. 绕过认证成功登录（直接返回系统首个用户身份）；后端使用字符串拼接 SQL 导致注入发生 | 浏览器登录表单提交比对 + 终端/Console SQL 查询日志查看 |
| **2. Reflected XSS** | 1. 输入普通关键词<br>2. 输入 XSS payload<br>(如 `<script>alert('XSS')</script>`) | 1. 正常显示搜索结果<br>2. Payload 作为转义文本安全显示 (`<%= %>`)；同时受 Content Security Policy (CSP) 头部防护 | 1. 正常显示搜索结果<br>2. Payload 脚本被浏览器直接解析执行（弹出 Alert 框 / 执行 JS） | 浏览器搜索框输入比对 + DOM Elements 元素审查 |
| **3. STRIDE Threat Model** | 检查 Session Cookie 安全配置、抗重放/重定向、防篡改 | Cookie 包含 `HttpOnly`, `SameSite=Lax`, `Path=/`, 高熵随机 Session ID；附带 CSRF Token；未登录/越权请求安全重定向 | Cookie 缺失 `HttpOnly` / `SameSite`；缺少 CSRF 校验；Session ID 可被脚本读取或篡改 | 浏览器 DevTools (`Application -> Cookies`, `Network Headers`) + 结合 `docs/login-stride-threat-model.md` 口头讲解 |
| **4. Database & Information Security** | 访问管理功能 (`/admin/*`)、检查审计日志 (`/admin/audit-logs`) | 强制身份认证与 Admin 角色校验 (`requireAdministrator`)；审计日志敏感数据掩码 (`[REDACTED]`)；DB 使用最小权限角色 (`food_ordering_app`) | 允许普通用户或未授权访问敏感管理接口/字段；日志明文暴露敏感信息；数据库超权连接 | DevTools 网络请求/页面权限测试 + DB 角色配置说明 + 结合 `docs/database-information-security.md` 讲解 |
| **5. Cryptography** | 检查密码存储哈希、强密码策略与 timing 保护 | 使用 `scrypt` + 16-byte 独立 salt 慢哈希；强密码校验策略；使用 `timingSafeEqual` 防范时序侧信道攻击 | 弱密码策略；使用弱哈希 (如 MD5/SHA1) 或明文/无 salt 存储；使用普通 `==` 比较密码 | 数据库密码 Hash 比对 + `src/security/passwords.js` 源码审查 + 结合 `docs/cryptography.md` 讲解 |

---

## 3. 改造成要与文件调整清单

### 3.1 端口与网络隔离
- 将 Vulnerable 应用监听端口由 3001 调整为 **3100**。
- Secure 保持 **3000** 端口。
- 两端均绑定 `127.0.0.1` 环回地址。

### 3.2 UI 与布局统一 (Northstar Kitchen Visuals)
- **共享设计语言**：两端统一使用 Northstar Kitchen CSS 变量、主色调、字体、按钮样式与响应式 Container。
- **导航栏 (Navbar)**：保持完全一致（主页 `/`, 菜单 `/menu`, 搜索 `/search`, 登录/账户 `/login`, 购物车 `/cart`）。**绝对不添加 `Security Demo` 菜单项**。
- **状态 Badge 标识**：
  - 在 `views/partials/header.ejs` (Secure) 顶栏右侧增加紧凑绿色 Badge：`SECURE APPLICATION · PORT 3000`。
  - 在 `demo/vulnerable/views/partials/header.ejs` (Vulnerable) 顶栏右侧增加紧凑红色 Badge：`INTENTIONALLY VULNERABLE · LOCAL DEMO · PORT 3100`。

### 3.3 Vulnerable 应用功能补全与同构 (demo/vulnerable/)
- **路由与视图匹配**：
  - `GET/POST /login`：提供与 Secure 端一致的登录界面，支持正常登录和受控的 SQLi 绕过演示。
  - `GET /search`：提供与 Secure 端一致的搜索界面，搜索结果模板使用未转义输出以触发 XSS。
  - `GET /admin/*` / `GET /admin/audit-logs`：提供缺少角色校验的越权访问对照点。
- **沙盒数据库与数据隔离**：
  - Vulnerable 端仅连接独立的 SQLite / 独立 demo DB，使用虚构数据集，绝不触碰 Secure PostgreSQL 生产/开发数据库。

### 3.4 启动与关闭脚本改进 (package.json & scripts)
- 更新 `package.json` 中的 demo 启动命令，指定端口 3100。
- 更新 `npm run shutdown` 脚本，记录并清理 3000 与 3100 两个端口的 PID 进程，确保可以一键干净关闭两个服务。

### 3.5 演示文档与 Runbook 同步
- 更新 `docs/demo-runbook.md` 与 `demo.md`：
  - 将 Vulnerable 端口全部统一更新为 `3100`。
  - 将演示步骤重排为双窗口直接对比 Runbook，引导演示者直接打开两个窗口进行输入比对，不再指导打开不存在的 `/security-demo` 页面。
  - 维持 `docs/*.md`（如 `login-stride-threat-model.md`, `database-information-security.md`, `cryptography.md`）作为项目离线文档和深读参考材料。

---

## 4. 验证计划

1. **双窗口功能验证**：
   - 同时启动 Secure (Port 3000) 和 Vulnerable (Port 3100)。
   - 在 Port 3000 与 Port 3100 上分别访问 `/login`, `/search`, `/menu`, `/admin/audit-logs`，确认页面结构与视觉风格完全一致。
2. **安全行为对比测试**：
   - 在两端 `/login` 输入 `' OR '1'='1`：确认 Port 3100 成功绕过登录，Port 3000 拒绝并提示错误。
   - 在两端 `/search` 输入 `<script>alert('XSS')</script>`：确认 Port 3100 弹窗/执行脚本，Port 3000 正常转义为纯文本。
   - 在两端打开 DevTools 检查 Session Cookie：确认 Port 3000 具备 `HttpOnly` 等安全属性，Port 3100 为不安全配置。
3. **自动化测试套件**：
   - 运行 `npm test` 确认 Secure 端现有的单元与集成测试全部通过，未受到任何影响。
4. **一键关闭测试**：
   - 运行 `npm run shutdown`，确认 3000 和 3100 端口服务均已干净停止。
